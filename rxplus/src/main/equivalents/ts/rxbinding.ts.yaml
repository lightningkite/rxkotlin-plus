- id: com.jakewharton.rxbinding4.view.clicks
  type: call
  template:
    pattern: 'fromEvent(~this~, "click", ev => ev.preventDefault())'
    imports:
      fromEvent: rxjs
- id: com.jakewharton.rxbinding4.view.longClicks
  type: call
  template:
    pattern: 'fromEvent(~this~, "contextmenu", ev => ev.preventDefault())'
    imports:
      fromEvent: rxjs